// App.tsx
// Renderizar la página principal `Index` ubicada en `src/pages/Index.tsx`
// (evita importar componentes que no existen en `src/components/`)
import Index from "@/pages/Index";

function App() {
  return <Index />;
}

export default App;
